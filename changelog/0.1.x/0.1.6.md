---
summary: "mcp-ts-core ^0.9.21: per-request log context fix, secret stripping from error messages, withRetry fail-fast on non-retryable errors; README client-config key renamed; skills sync"
breaking: false
security: false
---

# 0.1.6 — 2026-06-02

## Changed

- **`@cyanheads/mcp-ts-core`** `^0.9.16 → ^0.9.21` — adopts three framework fixes: per-request logs and traces now carry fresh request + trace/span IDs instead of the frozen boot context; `fetchWithTimeout` strips query-string secrets (e.g. `?api_key=`) from error messages and logs; `withRetry` fails fast on non-retryable errors and `ctx.fail` auto-populates the `retryable` flag.
- **README client-config key** renamed from `"socrata"` to `"socrata-mcp-server"` in all three install snippets (bunx, npx, Docker) for consistency with the package name.

## Dependencies

- `@cyanheads/mcp-ts-core` `^0.9.16 → ^0.9.21`
- `vitest` `^4.1.7 → ^4.1.8`
